export const DAY_HEIGHT = 11;
export const DAY_WIDTH = 11;
export const DAY_OFFSET = 4;

export const DAY_COLUMNS = 53;
export const DAY_ROWS = 7;

export const SVG_CANVAS_WIDTH = (DAY_COLUMNS * DAY_OFFSET) + (DAY_COLUMNS * DAY_WIDTH) - DAY_OFFSET;
export const SVG_CANVAS_HEIGHT = (DAY_ROWS * DAY_OFFSET) + (DAY_ROWS * DAY_HEIGHT) - DAY_OFFSET;
